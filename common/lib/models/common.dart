import 'package:fixnum/fixnum.dart';
import 'package:topl_protobuf/brambl/models/identifier.pb.dart';

// 32 bytes
typedef Eta = List<int>;

// 64 bytes
typedef Rho = List<int>;

typedef Slot = Int64;

typedef Epoch = Int64;

typedef TransactionId = Identifier_IoTransaction32;
